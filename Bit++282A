#include<stdio.h>
int main()
{
    int n,x=0,i;
    char a[5];

    scanf("%d",&n);

    for(i=0;i<n;i++)
    {
       scanf("%s",a);

       if(a[1]=='+')
       {
           x+=1;
       }
       else if(a[1]=='-')
       {
           x-=1;
       }
    }
    printf("%d\n",x);

    return 0;

}
