#include<iostream>
#include<iomanip>
using namespace std;
int main()
{
    int n, x;
    double area, sum = 0;
    cin >> n;
    for (int i = 0;i < n;i++)
    {
        cin >> x;
        sum += x;
    }
    area =  sum / n;
    cout << fixed << setprecision(12) << area << endl;
    return 0;
}
