#include<bits/stdc++.h>
using namespace std;
#define ll long long
int main()
{
    ll n;
    cin>>n;

    int arr[n+5];
    for(ll i=0; i<n; i++)
    {
        cin>>arr[i];

    }
    sort(arr,arr+n);
    for(ll i=0;i<n;i++)cout<<arr[i]<<" ";
    return 0;
}
