#include<bits/stdc++.h>
using namespace std;
#define Royscode() ios_base::sync_with_stdio(0);cin.tie(0);cout.tie(0);
#define endl '\n';
#define max1 10000
#define ll long long
int main()
{
    Royscode();
    int t;
    cin >> t;
    while(t--)
    {
        int a;
        cin >> a;

        if(a==1 || a==2 || a==4 || a==5 || a==8 || a==11)
        {
            cout << "NO" << endl;
        }
        else{
            cout << "YES" << endl;
        }
    }
    }
