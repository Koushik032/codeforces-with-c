#include<bits/stdc++.h>
using namespace std;

int main()
{
    int x1, y1, x2, y2;
    cin >> x1 >> y1 >> x2 >> y2;

    int diff = abs(x1-x2) + abs(y1-y2) + 2;
    if(x1 == x2)
        diff++;
    if(y1 == y2)
        diff++;

    cout << diff * 2 << endl;
}
