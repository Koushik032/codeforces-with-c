#include<bits/stdc++.h>
using namespace std;
#define Royscode() ios_base::sync_with_stdio(0);cin.tie(0);cout.tie(0);
#define endl '\n';
#define max1 10000
#define ll long long
int main()
{
    //Royscode();
    int t;
    cin >> t;
    while(t--)
    {
        int x,y,n;
        cin >> x >> y >> n;

        int z=abs(n-y)/x;

        int ans=(x*z)+y;

        cout << ans << endl;
    }
    return 0;
    }
