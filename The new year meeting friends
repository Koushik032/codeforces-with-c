#include<bits/stdc++.h>
using namespace std;
#define Royscode() ios_base::sync_with_stdio(0);cin.tie(0);cout.tie(0);
#define endl '\n';
#define max1 10000
#define ll long long
int main()
{
int arr[5];
for(int i=0;i<3;i++)
{
    cin >> arr[i];
}
sort(arr,arr+3);

int sum=arr[1]-arr[0];
int sum1=arr[2]-arr[1];

int distance=sum+sum1;

cout << distance << endl;
    
    return 0;
}
