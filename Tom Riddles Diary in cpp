#include<bits/stdc++.h>
using namespace std;
#define Royscode() ios_base::sync_with_stdio(0);cin.tie(0);cout.tie(0);
#define endl '\n';
#define max 10000
#define ll long long

int main() {
    Royscode();
    int t;
    cin >> t;
map<string,bool>vis;
    while(t--)
    {
        string s;
        cin >> s;

        if(vis[s] == 1)
        {
            cout << "YES" << endl;
        }
        else{
            cout << "NO" << endl;
        }
        
        vis[s] = 1;
    }
	// your code goes here
	return 0;
}
